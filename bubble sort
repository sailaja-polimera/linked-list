#include <stdio.h>
#include<stdlib.h>
int main()
{
    int a[]={7,3,9,5,2,6};
    int temp;
    for(int i=0;i<6;i++)
    {
         for(int j=0;j<(6-i);j++)
    {
        if(a[j]>a[j+1])
        {
            temp=a[j];
            a[j]=a[j+1];
            a[j+1]=temp;
            
        }
    }
    }
    for(int i=0;i<6;i++)
    {
        printf("%d ",a[i]);
    }
}
